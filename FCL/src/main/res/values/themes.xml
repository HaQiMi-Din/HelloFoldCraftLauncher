<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HelloFoldCraftLauncher" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color (HMCL Material You 蓝) -->
        <item name="colorPrimary">@color/hmcl_primary</item>
        <item name="colorPrimaryVariant">@color/hmcl_primary_container</item>
        <item name="colorOnPrimary">@color/hmcl_on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/hmcl_primary</item>
        <item name="colorSecondaryVariant">@color/hmcl_primary_container</item>
        <item name="colorOnSecondary">@color/hmcl_on_primary</item>
        <!-- 表面/错误色对齐 HMCL -->
        <item name="android:colorBackground">@color/hmcl_surface</item>
        <item name="colorError">@color/hmcl_error</item>
        <!-- Status bar color（沉浸式下与左侧栏压暗一致） -->
        <item name="android:statusBarColor">@color/hmcl_scrim_left</item>
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.Splash" parent="@style/Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.HelloFoldCraftLauncher</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>

    <style name="TabTextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="singleLine">true</item>
    </style>

    <style name="NavIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#FFF</item>
    </style>

    <!-- 原 FCLLibrary 模块样式（模块已并入 FCL） -->
    <style name="FCLCrashButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="android:layout_marginEnd">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="ripple">true</item>
    </style>

    <style name="FCLCrashTitle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@android:color/white</item>
        <item name="android:padding">10dp</item>
        <item name="auto_text_background_tint">true</item>
        <item name="auto_text_tint">true</item>
    </style>

    <style name="FCLCrashErrorText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="FCLCrashSplitLine">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:background">@android:color/darker_gray</item>
    </style>

    <style name="FCLCrashSidebarTitle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_marginEnd">10dp</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="FCLCrashSidebarHint">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
</resources>